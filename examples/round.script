// Calculate square root
module Sqrt

def main(a: Number, b: Number, c: Number): Number =
  let
    x = floor(a)
    y = ceil(b)
    z = round(c)
  in
    x+y+z


// Assert
def assert(): Bool = main(2.9, 5.1, 7.4) == 2+6+7